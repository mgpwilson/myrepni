# serverless.yml

service: myRepNIService

provider:
  name: aws
  runtime: nodejs10.x
  region: eu-west-1

functions:
  app:
    handler: build/index.handler
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'
